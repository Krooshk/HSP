addOne - increaseCount // Метод void для увеличения счетчика  
getMin - chooseMinimalNum // Выьрать наименьшее число из двух  
getFactorial - calcFactorial // Внутри происходит не просто чтение, а вычисление факториала числа  
odometer - calcTraversedPath // Считает пройденный путь  
removeZero - produceStringWithOnlyLetters // Преобразовать строку только с буквами  
findInRow - findSubstringInRow // Нахождение подстроки в ряду  
MaximumDiscount - calcMaxDiscount // Посчитать максимальную скидку  
compare - isSortedArray // Проверка - отсортирован ли массив  
secondMax - getSecondMax // Получить следующий элемент после максимального  
hashFun - calcHash // Посчитать хэш  
equals - isEquals // Эквиваленты ли множества  
mostFrequently - findMostFrequentlyElem // Найти наиболее повторяющийся элемент  