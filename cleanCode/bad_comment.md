```java
//...1 - пункт 12
private int jump; // кол-во пикселей
// ... Лучше поменять название отражающей действительность jumpLengthInPx
```

```java
//...2 - пункт 7
int kgSweetsCost = 200; // стоимость кг конфет в рублях
// ... Избыточный комментарий, лучше изменить название на kgSweetsCostRub
```

```java
//...3 - пункт 7
amountDollars = keyboard.nextInt(); // всего долларов
// ... Избыточный комментарий, можно убрать
```

```java
//...4 - пункт 12
oneDollar = keyboard.nextInt(); // курс доллара
// ... Можно убрать комментарий, название сделать USD_TO_RUB
```

```java
//...5 - пункт 4
// Импорт необходимых пакетов
// ... Шум
```

```java
//...6 - пункт 11
//  calculateJump(this) вызов внешней функции, которая рассчитывает траекторию прыжка, после приземления изменяется флаг isLand с помощью публичного метода setIsLand
// ... Закомментированный код
```

```java
//...7 - пункт 4
if (file.isDirectory()) { // Проверка на то, что файл - директория
// ... Шум
```

```java
//...8 - пункт 5
//-------------
// ... Позиционный маркер лучше удалить
```

```java
//...9 - пункт 2
// Червяк является самым базовым персонажем, поэтому можно создать экземпляр от класса Unit и не использовать отдельный класс для этого
// ... Слишком много информации, стот сократить 
```

```java
//...10 - пункт 2
zipEntry = zipInputStream.getNextEntry(); // Пытаемся получить следующую сущность в архиве
// ... "Пытаемся", следует написать более строго без лишней воды, также из кода понятно, что происходит
```

```java
//...11 - пункт 1
// Важно обрабатываеть все случаи:
// ... Какие конкретно случаи(когда элемент в начале, в конце, в середине, когда элемент один)
```

```java
//...12 - пункт 8
        while ((length = fileInputStream.read(buffer)) > 0) { // Пытаемся записать данные в buffer, если получаем 1, значит успешно записали, иначе выходим
// ... Слишком много информации
```

```java
//...13 - пункт 9
File root = new File("."); // Устанавливается связь с файлом, в linux файлом является также директория
// ... В коде информация системного уровня: " в linux файлом является также директория"
```

```java
//...14 - пункт 7
byte[] buffer = new byte[1024]; // Задаем массив из байтов размером 1024
// ... Избыточный комментарий
```

```java
//...15 - пункт 2
// Здесь просто добавляю счетчик для счета и при дешевых операций добавляю разницу (3 - 1) в копилку, при реалокации вычитаю за каждую операцию 1.
// ... Комментарий нестрогий, есть лишние слова("просто"), оно занимает место, но не несет никакого смысла
```