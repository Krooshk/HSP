```java
//...1
        assert pos >= 0;
// ...Добавил инвариант, что индекс массива всегда положительныййй
```

```java
//...2
        assert factorial >= 1;
// ...Добавил инвариант, что факториал всегда больше или равен 1
```

```java
//...3
        assert timeSpent >= 0;
// ...Добавил инвариант, потраченное время не может быть отрицательным
```

```java
//...4
        for (; map.size() != size; ) {
// ...Избавился от while цикла, использовал for не со всеми параметрами 
```

```java
//...5
        for(;copyStr.length() > 0;) {
// ...Избавился от while цикла, использовал for не со всеми параметрами
```

```java
//...6
        final static int DEFAULT_VALUE = 23;
// ...Вынес переменную в константу
```

```java
//...7
        int debt = 0;
// ...Перенёс переменную ближе к месту использования
```

```java
//...8
        double percent = Votes[indexMax] * 1.0 / sumVotes;
// ...Перенёс переменную ближе к месту использования
```

```java
//...9
        commandNumber = -1;
// ...Завершив работу с переменной, присвоил ей несуществующий код.
```

```java
//...10
        int result = 0;
// ...Перенёс переменную ближе к месту использования
```

```java
//...11
        boolean isGrowth = true;
// ...Перенёс переменную ближе к месту использования
```

```java
//...12
        boolean isWalker = village.charAt(i) == '=';
// ...Перенёс переменную ближе к месту использования
```

```java
//...13
        for (;node1 != null;) {
// ...Избавился от while цикла, использовал for не со всеми параметрами
```

```java
//...14
        int count = step;
// ...Перенёс переменную ближе к месту использования
```

```java
//...15
        final int INITIAL_VALUE = 0;
// ...Вынес в константу начальное значение битового массива
```

        